<!DOCTYPE html>
<html>
<% include ./partials/head %>

    <body>
        <img src="<%= gifUrl %>" alt="giphy!">
        <br>

        <span class='mainMenu'>
            <form action="./">
                <% for (var i = 0; i < quoteList.length; i++) { %>
                    <p><%=quoteList[i].sentence %></p>
                    <br> <p>by <%=quoteList[i].name %></p>
                <% } %>
                <br> <input type="submit" class="button" value="Start New Session"> 
            </form>
        </span>
        <% include ./partials/scripts %>
    </body>

</html>